{
    "name": "ashwin-portfolio-unified",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix v2-glassmorphic",
        "dev:v1": "npm start --prefix v1-legacy",
        "build": "bash build_unified.sh",
        "serve": "npx serve dist",
        "deploy": "npm run build && npx gh-pages -d dist"
    }
}